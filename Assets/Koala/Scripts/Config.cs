public static class Constants
{
	// Use this epsilon for schedule the occurrences so
	// the start and end time of occurrences in two cycle
	// don't reach each other
	public static readonly float OCCURRENCE_DELAY = 1e-6f;
}
